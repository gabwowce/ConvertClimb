name: 🚀 Expo Deploy

on:
  push:
    branches:
      - main # arba master, jei naudoji senesnį pavadinimą

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🧱 Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔐 Authenticate with Expo
        run: npx expo login --token ${{ secrets.EXPO_TOKEN }}

      - name: 🚀 Publish to Expo
        run: npx expo publish --non-interactive
